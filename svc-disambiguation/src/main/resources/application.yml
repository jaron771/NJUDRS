server:
  port: 8883
eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

# MyBatis
mybatis:
  typeAliasesPackage: com.nju.disambiguation.po
#  mapperLocations: classpath:mapper/**/**.xml
#  configuration-properties: spring.datasource.ieee.*
